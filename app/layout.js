export const metadata = {
  title: 'Aiva Liu — Virtual Influencer',
  description: 'Fictional virtual influencer concept with Supabase auth demo.',
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
